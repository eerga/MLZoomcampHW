## 5.1 Intro / Session Overview
- What we will cover this week

## 5.2 Saving and loading the model

- Saving the model to pickle
- Loading the model from pickle
- Turning our notebook into a Python script

## 5.3 Web services: introduction to Flask

- Writing a simple ping/pong app
- Queryting it with `curl` and browser

## 5.4 Serving the model with Flask
- Wrapping the predict script into a Flask app
- Querying it with `requests`
- Preparing for production: gunicorn
- Running it on Windows with waitress

## 5.5 Dependency and environment management: Pipenv
- Why we need virtual environment
- Installing Pipenv
- Installing libraries with Pipenv
- Running things with Pipenv

## 5.6 Environment Management: Docker
- Why we need Docker
- Running a Pythong image with Docker
- Dockerfile
- Building a docker image
- Running a docker image

## 5.7 Deployment to the cloud: AWS Elastic Beanstalk (optional)
- Installing the eb cli
- Running eb locally
- Deploying the model

## 5.8 Exploring more
- Flask is not the only framework for creating web services. Try others, i.e. FastAPI
- Experiment wiht other ways of managing environment, i.e. virtual env, conda, poetry
- Explore other ways of deploying web services, i.e. GCP, AZure, Heroku, Python Anywhere, etc.

## 5.9 Summary
- Save models with pickle
- Use Flask to turn the model into a web service
- Use dependency and env manager
- Package it in Docker
- Deploy to the cloud

